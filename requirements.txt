# Multi-backend OCR System - Shared Dependencies
# This file contains ONLY truly shared dependencies
# Backend-specific dependencies are handled in their own setup scripts

# Core dependencies (minimal shared set)
flask==2.3.3
flask-cors==4.0.0
Pillow==10.0.1
numpy==1.24.3
requests==2.31.0

# PDF processing
PyMuPDF==1.23.8
img2pdf==0.4.4

# Utilities
einops==0.7.0
easydict==1.10
addict==2.4.0

# NOTE: GPU/ML dependencies (torch, vllm) are handled by each backend
# to avoid version conflicts and ensure proper GPU compatibility